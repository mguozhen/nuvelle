# 四个前端服务共用的静态站点镜像。
# Cloud Build 通过 SITE_DIR 传入具体构建产物目录，然后交给 Nginx 托管。
FROM nginx:1.27-alpine

ARG SITE_DIR

# 把前端构建产物复制到 Nginx 默认站点目录。
COPY ${SITE_DIR}/ /usr/share/nginx/html/
COPY deploy/nginx-static.conf /etc/nginx/conf.d/default.conf

EXPOSE 8080
